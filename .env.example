# 이 파일을 복사해서 .env 로 만들어 사용하세요.
#   복사 명령(Windows): copy .env.example .env

# CORS 허용 출처 (스프링 서버 주소). 여러 개면 콤마로 구분. * 는 전체 허용.
#   예) CORS_ORIGINS=http://localhost:8080
CORS_ORIGINS=*

# 스프링부트만 이 서버를 호출하도록 막을 때 쓰는 공유 키 (선택, 비우면 검사 안 함)
SERVICE_API_KEY=
